# Supabase PostgreSQL transaction/session pooler connection string for server-side use only.
# Never expose this value through NEXT_PUBLIC_*.
DATABASE_URL=postgresql://postgres.PROJECT_REF:PASSWORD@HOST:6543/postgres?sslmode=require
